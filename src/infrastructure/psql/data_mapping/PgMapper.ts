const pgMapper = {
    // toOrder(entity: PgOrder): OrderResponse {},
    // toProduct(entity: PgProduct): ProductResponse {},
    toUser(entity: PgUser): UserResponse {},
    // toOrderProduct(entity: PgOrderProduct): OrderProductResponse {},
};

export default pgMapper;
